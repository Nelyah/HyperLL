This project has several main it can use.

fromInput.c
bench3_64.c

The 32 bits version are deprecated

If compiling with fromInput.c, run as 
./hyperLL < file_of_input.txt

If compiling with bench3_64.c
specify the two arguments : 
    1rst : the step
    2nd : the number of experiments
